You'll have to uncompress and untar 'master.tar.gz' (and then delete it)
before you can make the bootkernels. Make sure you do this as root, so the
file ownerships and devices will be extracted correctly.
